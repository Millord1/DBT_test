version: 2

# Les tables de la base de collecte (brief 03), schema public.
# Une table declaree ici s'ecrit {{ source('megabase0', 'commune') }} dans un
# modele. A completer : ajoutez vos typologies sous "tables".
sources:
  - name: "{{env_var('DB_NAME')}}"
    schema: public
    tables:
      - name: commune
      - name: departement
      - name: region
      - name: lycee
      - name: clubs_boxe_thai
      - name: ehpad
      - name: mairie
      - name: pharmacie
      - name: bibliotheque
      - name: college
